name: CI Example
on: [push]

jobs:
  build:
    runs-on: Lix-ubuntu
    
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        
      - name: Run a build script
        run: echo "Building on my own infrastructure!"
